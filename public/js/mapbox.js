/* eslint-disable */

// instead of making separate call for api for locations we can just set attribut data and we can use it here
// to access data attribute just dataset.NAME
// const locations = JSON.parse(document.getElementById('map').dataset.locations);
// console.log(locations);

